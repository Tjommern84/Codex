import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'no.pustyoga.app',
  appName: 'Pust Yoga',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
}

export default config
